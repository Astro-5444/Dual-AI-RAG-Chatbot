# Dual-AI RAG Chatbot requirements
# Install with: pip install -r requirements.txt

# Terminal UI
rich>=13.0.0

# PDF parsing
PyMuPDF>=1.24.0

# Embeddings (local, ~90MB download on first run)
sentence-transformers>=3.0.0

# Vector store
chromadb>=0.5.0

# HTTP client (for calling your llama-server proxy)
httpx>=0.27.0

# Progress bars
tqdm>=4.66.0

# GPU acceleration
torch>=2.0.0

# Gradio UI (optional, for legacy chatbot.py)
gradio>=4.40.0
